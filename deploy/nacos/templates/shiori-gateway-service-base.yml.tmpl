spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

feature:
  api-v2:
    enabled: true

security:
  auth:
    whitelist:
      - /actuator/health/**
      - /v3/api-docs/**
      - /swagger-ui/**
      - /swagger-ui.html
      - /api/user/auth/register
      - /api/user/auth/login
      - /api/user/auth/refresh
      - /api/user/auth/logout
    anonymous-get-paths:
      - /api/product/**
      - /api/v2/product/**
      - /api/user/profiles/**
      - /api/user/media/avatar/**
      - /api/v2/product/users/**
  rate-limit:
    enabled: true
    login-per-second: 8
    order-create-per-second: 10
    order-pay-per-second: 15
  authz:
    user-service-base-url: ${SECURITY_AUTHZ_USER_SERVICE_BASE_URL}
    cache:
      redis:
        enabled: ${SECURITY_AUTHZ_CACHE_REDIS_ENABLED:true}
        key-prefix: ${SECURITY_AUTHZ_CACHE_REDIS_KEY_PREFIX:authz:snapshot:}
        ttl-seconds: ${SECURITY_AUTHZ_CACHE_REDIS_TTL_SECONDS:30}
        stale-ttl-seconds: ${SECURITY_AUTHZ_CACHE_REDIS_STALE_TTL_SECONDS:300}
    event:
      enabled: ${SECURITY_AUTHZ_EVENT_ENABLED:true}
      redis-channel: ${SECURITY_AUTHZ_EVENT_REDIS_CHANNEL:shiori.authz.user.changed}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
