spring:
  datasource:
    username: ${SOCIAL_DB_USERNAME}
    password: ${SOCIAL_DB_PASSWORD}
  rabbitmq:
    username: ${SOCIAL_RMQ_USERNAME}
    password: ${SOCIAL_RMQ_PASSWORD}

storage:
  oss:
    access-key: ${MINIO_PRODUCT_ACCESS_KEY}
    secret-key: ${MINIO_PRODUCT_SECRET_KEY}
