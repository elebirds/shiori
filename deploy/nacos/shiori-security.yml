security:
  jwt:
    # 至少 32 字节随机字符串，示例仅用于本地开发
    hmac-secret: "replace-with-your-32+bytes-secret"
    issuer: "shiori"
    access-ttl-seconds: 900
    refresh-ttl-seconds: 604800
  gateway-sign:
    internal-secret: "replace-with-your-internal-sign-secret"
    max-skew-seconds: 300
